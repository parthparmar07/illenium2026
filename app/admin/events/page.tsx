import { createClient } from "@/lib/supabase/server"; import { RoleShell } from "@/components/layout/role-shell";
export default async function EventsAdminPage(){const supabase=await createClient();const {data}=await supabase.from("events").select("id,name,category,venue,status,capacity,event_registrations(count)").order("start_time");return <RoleShell role="admin"><div className="workspace-page-head"><div><div className="workspace-kicker">Programme control</div><h1>Event management</h1><p className="workspace-subtitle">Keep the public programme, capacity and check-in scope aligned.</p></div></div><div className="workspace-panel table-wrap"><table><thead><tr><th>Event</th><th>Category</th><th>Venue</th><th>Capacity</th><th>Status</th><th>Registered</th></tr></thead><tbody>{data?.map(row=><tr key={row.id}><td><strong>{row.name}</strong></td><td>{row.category}</td><td>{row.venue}</td><td>{row.capacity??"—"}</td><td><span className="status-pill status-success">{row.status}</span></td><td>{Array.isArray(row.event_registrations)?row.event_registrations[0]?.count??0:0}</td></tr>)}</tbody></table>{!data?.length&&<p className="workspace-subtitle">No events configured yet.</p>}</div></RoleShell>}
