import Link from "next/link"; import { createClient } from "@/lib/supabase/server";
export default async function EventsAdminPage(){const supabase=await createClient();const {data}=await supabase.from("events").select("id,name,category,venue,status,capacity,event_registrations(count)").order("start_time");return <div className="app-shell"><header className="app-nav"><Link href="/admin/dashboard" className="brand"><span className="brand-mark"><span>✦</span></span> ILLENIUM 2026</Link><span>EVENTS</span></header><main className="app-main"><h1 style={{fontSize:"2.5rem"}}>Event management</h1><div className="panel table-wrap"><table><thead><tr><th>Event</th><th>Category</th><th>Venue</th><th>Capacity</th><th>Status</th><th>Registered</th></tr></thead><tbody>{data?.map(row=><tr key={row.id}><td>{row.name}</td><td>{row.category}</td><td>{row.venue}</td><td>{row.capacity??"—"}</td><td><span className="status green">{row.status}</span></td><td>{Array.isArray(row.event_registrations)?row.event_registrations[0]?.count??0:0}</td></tr>)}</tbody></table></div></main></div>}
