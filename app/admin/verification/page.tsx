import { createClient } from "@/lib/supabase/server"; import { approveParticipant } from "@/services/participant-service";
export default async function VerificationPage(){const supabase=await createClient();const {data}=await supabase.from("participants").select("id, illenium_id, college_roll_number, verification_status, registration_status, profiles(full_name, email), colleges(name)").eq("verification_status","pending").order("created_at");return <div className="app-shell"><header className="app-nav">ILLENIUM 2026 <span>VERIFICATION QUEUE</span></header><main className="app-main"><h1 style={{fontSize:"2.5rem"}}>Pending verification</h1><div className="panel table-wrap"><table><thead><tr><th>Name</th><th>College</th><th>Roll number</th><th>Action</th></tr></thead><tbody>{data?.map((row)=>{const profile=Array.isArray(row.profiles)?row.profiles[0]:row.profiles;const college=Array.isArray(row.colleges)?row.colleges[0]:row.colleges;return <tr key={row.id}><td>{profile?.full_name}</td><td>{college?.name}</td><td>{row.college_roll_number}</td><td><form action={approveParticipant}><input type="hidden" name="participantId" value={row.id}/><button className="btn btn-primary">Approve & generate ID</button></form></td></tr>})}</tbody></table>{!data?.length&&<p className="muted">No pending participants.</p>}</div></main></div>}
