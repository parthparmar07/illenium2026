"use client";
import { useState } from "react"; import Link from "next/link"; import { createClient } from "@/lib/supabase/client";
export default function LoginPage() { const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [message,setMessage]=useState(""); async function login(){ const {error}=await createClient().auth.signInWithPassword({email,password}); if(error){setMessage(error.message);return;} window.location.href="/participant/dashboard"; } return <main className="auth-shell"><div className="auth-card"><Link href="/" className="brand"><span className="brand-mark"><span>✦</span></span> ILLENIUM 2026</Link><h1 className="display" style={{ fontSize:"3rem", marginBottom:".5rem" }}>Welcome back.</h1><p className="muted">Access your ILLENIUM ID or operations console.</p><div style={{ display:"grid", gap:"1rem", marginTop:"2rem" }}><label className="field" style={{ color:"white" }}>Email<input className="input" value={email} onChange={e=>setEmail(e.target.value)} /></label><label className="field" style={{ color:"white" }}>Password<input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} /></label>{message&&<p style={{color:"#ff9aa5"}}>{message}</p>}<button className="btn btn-primary" onClick={login}>Log in</button><p className="muted" style={{fontSize:".85rem"}}>New here? <Link href="/register" style={{color:"var(--blue)"}}>Start registration</Link></p></div></div></main>; }
