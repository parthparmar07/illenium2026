import Link from "next/link"; import QRCode from "qrcode"; import { createClient } from "@/lib/supabase/server"; import { decryptToken } from "@/lib/qr/secure-token"; import { qrUrl } from "@/lib/qr/token";
export default async function DigitalIdPage(){ const supabase=await createClient(); const {data:{user}}=await supabase.auth.getUser(); let data:{full_name?:string;college?:string;illenium_id?:string;photo_url?:string|null;token_ciphertext?:string}|null=null; if(user){const {data:row}=await supabase.from("participants").select("illenium_id, profiles!inner(full_name, photo_url), colleges(name), qr_tokens(token_ciphertext)").eq("profiles.user_id",user.id).maybeSingle(); if(row){const profile=Array.isArray(row.profiles)?row.profiles[0]:row.profiles;const college=Array.isArray(row.colleges)?row.colleges[0]:row.colleges;const qr=Array.isArray(row.qr_tokens)?row.qr_tokens[0]:row.qr_tokens;data={full_name:profile?.full_name,college:college?.name,illenium_id:row.illenium_id,photo_url:profile?.photo_url,token_ciphertext:qr?.token_ciphertext};}} let qrData=""; if(data?.token_ciphertext){try{qrData=await QRCode.toDataURL(qrUrl(decryptToken(data.token_ciphertext)),{width:420,margin:1});}catch{qrData="";}} return <div className="app-shell" style={{background:"#080810",color:"white",minHeight:"100vh"}}><header className="app-nav"><Link href="/participant/dashboard" className="brand"><span className="brand-mark"><span>✦</span></span> ILLENIUM 2026</Link><span>DIGITAL ID</span></header><main style={{padding:"2rem 1rem",display:"grid",placeItems:"center"}}><div className="id-card"><div className="id-card-top"><span>ILLENIUM / 26</span><span>Verified pass</span></div><div style={{display:"flex",alignItems:"center",gap:"1rem",margin:"2rem 0 1rem"}}><img className="id-photo" src={data?.photo_url??"/favicon.svg"} alt="Participant profile"/><div><h1 style={{margin:0,fontSize:"1.5rem"}}>{data?.full_name??"Participant"}</h1><p className="muted" style={{margin:".3rem 0"}}>{data?.college??"College pending"}</p><strong style={{color:"var(--blue)",letterSpacing:".12em"}}>{data?.illenium_id??"ID pending"}</strong></div></div><div style={{borderTop:"1px solid var(--line)",paddingTop:"1rem",display:"flex",justifyContent:"space-between",alignItems:"end"}}><div><div className="muted" style={{fontSize:".7rem"}}>STATUS</div><span className="status green">Approved</span></div>{qrData?<img className="id-qr" src={qrData} alt="Secure ILLENIUM QR"/>:<p className="muted" style={{maxWidth:150,fontSize:".75rem"}}>Your secure QR appears after an admin approves your registration.</p>}</div></div></main></div>; }
